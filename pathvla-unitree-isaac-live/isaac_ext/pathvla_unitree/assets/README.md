# Assets

Place project-specific asset notes here.

This repo does not redistribute the Unitree G1 USD asset. Set:

```bash
export UNITREE_G1_USD_PATH=/absolute/path/to/unitree_g1.usd
```

If that path is missing, strict runs fail unless `--allow-proxy` is passed explicitly.
